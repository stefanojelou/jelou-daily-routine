# Open question backlog

The daily routine pulls a few of these each run (rotated) and appends new ones as
findings raise fresh questions. Answered questions get struck through (`- ~~...~~`)
so they drop out of rotation but stay as a record.

Format: one question per `- ` bullet. Keep them specific and data-answerable.

## Acquisition
- Which acquisition channel (initial_utm_source) has the best signup→activation→pay full-funnel yield, not just signup volume?
- ~~Is the mobile-vs-desktop activation gap widening or narrowing across recent signup cohorts?~~ (2026-07-06: gap is near-total — mobile ~0.3% build vs desktop ~29-31%.)
- Do signups that arrive with `has_template=true` at onboarding_started convert better than blank-start signups?
- ~~What share of `onboarding_started` never reach `signup_completed`, and where do they die?~~ (2026-08-07: overall 44% complete within 1d (June, prod, unique); it dies unevenly BY CHANNEL — organic/direct 50% (970/1,951) vs Google paid ~40%, Facebook 34%, X ~26%, Instagram 24% — measured on the EVENT-level utm on onboarding_started. WARNING: the initial_utm_source *profile* breakdown inverts this (media ~94%) via back-stamping and must not be used. Step-WITHIN-signup death (SSO vs email/password) is the Jul-29 findings.)
- Is the organic > paid land→signup completion gap (50% vs 24-41%) a device-composition effect (media is ~77% mobile) or intrinsic to the channel? Split onboarding_started→signup_completed by device WITHIN each channel — if mobile-organic also completes ~50% and mobile-paid ~25%, it's the channel/creative, not the device.
- Instagram is the worst land→signup channel (24%) AND near-worst at build-activation (Jul-9) — is IG traffic simply mis-targeted (wrong audience/creative promising the wrong thing), and does any IG placement/creative complete signup at the organic rate?
- Does the channel that completes signup best (organic) also ACTIVATE and RETAIN best, or does a lower-completing paid channel (e.g. Google search) yield higher-intent finishers? Chain onboarding→signup→node_used/agent_message→credit_topup by event-level channel for a full-funnel yield-per-entrant (pay leg needs Stripe).
- Reconcile the two channel reads: media completes signup at HALF organic (this finding, event-utm) yet out-messages organic on the widest net (Jul-30, 70% vs 63%, profile-utm). Is the Jul-30 media>organic activation edge ALSO a profile-utm back-stamping artifact? Re-run the Jul-30 activation funnel with EVENT-level utm to check whether media's apparent activation edge survives the unbiased cut.

## Activation
- ~~What is the median time from signup_completed to first agent_message_sent, and how does it differ for eventual payers?~~ (2026-07-17: median is under 1h — 56% of ALL signups message within 1h, 60% by 1d, 65% by 30d (clean May18–Jun30 cohort, N=3,697). Eventual credit-toppers too small-N (51) to split by speed reliably; revisit with Stripe payers.)
- Agent-message activation (65%) dwarfs build activation (13%) on the same cohort — which one actually predicts week-2/week-4 return and credit_topup? Is messaging a low-bar vanity activation or the real leading indicator?
- Mobile signups converse at ~60% (near desktop 69%) but build at ~2.9%. Do mobile-signup messagers RETAIN (wk2+), or do they message once and vanish — i.e. is mobile conversation real activation or a dead-end without the builder?
- ~~Which single activation milestone most strongly predicts week-2 return?~~ (2026-08-10: BUILD or TEST, not chat. Cohorting on each milestone event and measuring next-week return to a neutral signal (app_session_started), matched Jun 17-Jul 20 window: node_used-first 74.5% (1,225/1,644), tester_session_started-first 66.0% (748/1,133), agent_message_sent-first only 45.7% (951/2,080) — a ~29pt build-vs-chat gap; same ranking on agent_message_sent return (32/27/24%). The widest net (agent chat, 65% of signups) is the weakest predictor. tester launched ~Jun 17 so it's confound-free and still beats chat by ~20pt. Marks not drives.)
- ~~How large is the dead-on-arrival cohort (signed up, zero activation events), and which channel produces it most?~~ (2026-07-06: ~55% of signups are mobile and only ~0.3% ever build; paid channels are 77% mobile → 5-7% build-activation vs 15% organic.)
- Do the mobile signups ever come back on desktop later (cross-device activation), or are they permanently lost? (split by distinct_id)
- Is there a mobile-appropriate first action (chat/tester) we could make the mobile activation goal instead of the desktop-only canvas?
- What is the CAC-adjusted view: does paid spend on mobile-heavy channels (Meta, Google) ever recover given ~5% build-activation?
- Given 86% of build-activators fire within 24h of signup, does the ~14% "slow" cohort (day 1-14) retain/pay any differently than the same-session builders — i.e. is a delayed first build a worse or equal signal?
- Does the same first-hour front-loading hold for the OTHER milestones (skill_created, tester_session_started, agent_message_sent), or is node_used uniquely same-session — and which milestone has the longest tail worth a re-engagement nudge?
- Builder self-retention (43% wk1, ~32% wk6) is ~2x agent-message self-retention (24% wk1, ~16% wk6) — but node_used has long pre-window history so builder births may include veterans. Re-run on a TRUE first-ever-builder cohort (server-side / DB user-created join) to confirm the gap magnitude isn't an event-age artifact.
- Do the users who do BOTH (build AND message) retain better than either single-surface cohort — is multi-surface activation the real stickiness signal rather than which surface?
- On the WIDEST net media out-activates organic (70% vs 63% message) yet builds far less (7% vs 15%) — do these high-messaging media signups RETAIN (wk2/wk4) and top up credits, or is their conversation a low-bar activation that never converts? (the real test of whether paid spend is wasted or just mis-measured)
- Why did organic 30d agent-message activation fall from 70% (May 18–Jun 8) to 47% (Jun 9–30) while media rose 59%→76%? Confirm it's real (not window-boundary/attribution) and test the free-limit-cut ~Jun 11 as the cause.
- Size the truly-dead-on-ALL-surfaces cohort exactly (never node_used AND never agent_message_sent AND never tester_session_started AND never skill_created) with a proper user-level union — needs JQL groupByUser or a DB join; the ~33-35% here is a widest-net upper-bound.
- Reframe of the reach-vs-depth tension: agent activation reaches ~5x more signups (65% vs 13%) but retains ~2x worse per user; net wk1-retained share is ~15.6% (agent) vs ~5.6% (builder) of signups. Which surface should onboarding optimize FOR — the wider net or the deeper hook — and does a build nudge for the messager cohort lift their retention toward the builder curve?

## Templates
- Does installing a template shorten time-to-activation, or just correlate with users who'd activate anyway?
- Which specific templates lead to real downstream usage vs sit unused (shelfware)?
- ~~What fraction of template *preview* opens convert to an actual install?~~ (2026-07-13: 18% overall — 27 of 147 unique previewers install any template within 30d; per-template 0–36%.)
- Why do "Appointment scheduling" (0/13) and "Schedule with Microsoft Outlook Calendar" (0/6) convert 0% preview→install while "Schedule with Google Calendar" converts 12% — broken install path, missing integration, or genuinely unwanted?
- Most installs come from ~40 custom/Spanish templates NOT in the previewable gallery (Agente Shopify 39, Tienda Jelou Shop 14) — how are those discovered/installed if not via the preview gallery, and should the top custom ones be promoted into the gallery?
- Of the 27 gallery-preview installers, how many go on to build (node_used) vs the ~40 off-gallery installers — does gallery provenance predict activation?
- Template-install count is a monotonic dose-response marker of agent/build depth, but the JUMP is at the 2nd install (1 install ≈ non-installer, median 4 vs 3; 2+ ≈ median 14+, mean ~62). What triggers a 2nd install — is there a product nudge that converts single-installers (58% of installers) into multi-installers, and would that raise depth or merely re-mark already-deep users?
- Is the install→depth link TIME-ORDERED (installs precede the deep usage) or do deep users accumulate installs afterward? Needs event-time sequencing / DB timestamps to separate mark-vs-drive on templates the way the Jun 24 pay finding couldn't.
- Do the ~40 multi-install (2+) users retain (wk2/wk4) and monetize (credit_topup) better than single-installers and non-installers, or is depth-in-window all they share?

## Monetization
- What is the most common action sequence in the 48h before a company's first real (amount>0) charge? (2026-08-03 partial, credit_topup analogue in Mixpanel: the last action before a credit_topup is most often an agent conversation, not a billing-page visit — 44% of toppers' immediate predecessor is agent_response_received and 52 went message→response→topup with NO billing page in the last two steps, vs ~24 via the deliberate page_visited_settings_billing→add-credits path (conversation ~2x billing). Top-ups are 85% desktop. Real amount>0 first charge still needs Stripe.)
- Do the ~52 "hit-the-wall-mid-chat" toppers (conversation→topup) differ from the ~24 deliberate billing-page toppers in top-up AMOUNT, repeat-topup rate, or subsequent retention — is a reactive out-of-credits top-up smaller/stickier than a planned one?
- If ~2 in 5 top-ups originate inside a conversation with no billing-page visit, is there an in-chat "out of credits → top up" prompt firing, and what's its click→topup rate vs the billing-page path? (needs the in-conversation upsell event, if instrumented.)
- Credit top-up is 85% desktop despite mobile being ~half of signups and conversing at 60% — do mobile conversers simply never hit the credit wall (lighter usage) or do they hit it and fail to top up on mobile (checkout/UX friction)? Split credit-wall exposure vs top-up by device.
- ~~Do credit top-up buyers or first-time subscribers retain/expand better over the following month?~~ (2026-07-14: not answerable in Mixpanel yet — subscription_changed only started firing ~Jun 24 2026, so all 14 subscribers have <3wk immature windows. Credit-toppers alone: agent-activity retention decays 46%→14%→6% over wk0-2. Revisit once subscription cohorts mature / Stripe is wired.)
- ~~What is the realized conversion from `upgrade_cta_clicked` to an actual paid charge, by source?~~ (2026-07-23: not answerable — `upgrade_cta_clicked` is a just-shipped event: 0 firings Jul 2025–Jun 2026, only 7 firings/6 users in Jul 2026; `subscription_changed` also newborn (~Jun 24, 20 users/90d). The real, high-volume upgrade-intent signal is `page_visited_settings_billing` (1,187 users/90d) → only ~6% top up credits within 30d. Revisit the CTA→paid funnel once both events mature.)
- The billing-page → credit_topup conversion is ~6% overall (72/1,177, 30d). Is the ~94% who view billing but never top up dominated by *sticker-shock* (they see the price and leave) or by *checkout friction* (they intend to but the pre-checkout redirect fails)? credit_topup fires pre-redirect, so a `credit_topup` with no matching Stripe charge would isolate friction — needs Stripe.
- Does viewing the billing page REPEATEDLY (2+ `page_visited_settings_billing`) convert to topup materially better than a single visit — i.e. is repeat billing-intent a stronger pay signal worth a nudge?
- Do the ~6% of billing-viewers who top up differ from the 94% who don't on prior usage depth (agent_message_sent / node_used count) — is monetization intent just a proxy for engagement depth, like template installs and credit-buying already are?
- ~~Now that subscription_changed is live (~Jun 24 2026), re-run credit-topup vs subscription retention once the subscriber cohort has a mature ≥4-week window (≈late Jul).~~ (2026-08-12: attempted on the co-existing window Jun 22-Aug 12. On the agent-activity proxy, credit-toppers out-retain subscribers at every early week (wk0 60% vs 26%, wk1 24% vs 6%, wk2 23% vs 6%). BUT the proxy is invalid for subscribers — only 26% of subscribers send an agent message even in their subscribe-week, so subscribers are plausibly a managed/MRR motion whose retention agent_message_sent can't see; true subscription retention needs Stripe. Populations still tiny (topup 35 / sub 25), so the exact rates are directional — revisit when N grows or Stripe is wired.)
- Subscribers barely use the agent (only ~26% send an agent message in their subscribe-week vs 60% of credit-toppers). Are subscribers systematically a managed/sales-sold/enterprise cohort (vs self-serve credit-toppers), and does their value show up on OTHER surfaces (node_used, campaign_sent, tester) rather than agent chat? Split the 25 subscribers by self_service_cohort / company size / non-agent event activity to test the "subscribers are a different motion" hypothesis.
- Which non-agent event best proxies subscriber retention (app_session_started, node_used, campaign_sent)? Re-run subscription_changed retention against each to find a return signal that isn't blind to the subscriber motion.
- Do the ~79%-organic credit-toppers differ from the ~21% media ones in top-up amount, repeat-topup rate, or agent-activity retention — i.e. does channel predict credit-buyer quality?
- ~~Are credit-toppers and subscribers genuinely disjoint populations, or a funnel? (lifetime funnel showed 3% topup→sub and 14% sub→topup, but both are confounded by subscription_changed's Jun-24 start — re-test on the co-existing window.)~~ (2026-08-12: DISJOINT, not a funnel. On the confound-free co-existing window (Jun 22-Aug 12, prod, unique, 45d): topup→sub 6% (2/35), sub→topup 8% (2/25) — ~94% of each group never touches the other. Near-symmetric and tiny, refining the lifetime 3%/14%. The rare topup→sub crossovers took ~11.9d; sub→topup crossovers were same-session (~28min), both N=2.)
- The two monetization motions are ~94% disjoint AND near-symmetric in size (35 toppers vs 25 subscribers). Do they map to different geographies/segments (e.g. is subscription an Ecuador/managed play and credit-topup the broader self-serve LatAm behavior)? Split each population by mp_country_code / initial_utm_source.
- What do credits actually get spent on (agent_message_sent vs campaign_sent vs other) — validate agent_message_sent as the right post-topup retention proxy.

## Retention
- ~~Are recent signup-month cohorts retaining better or worse than older ones at week 2 and week 4?~~ (2026-08-04: at WEEK 1, no decay — weekday-birth wk1 self-retention flat May 22.8%→Jun 27.0%→Jul 25.7% (cohort on first agent_message, since signup_completed is collapsed); wk2/wk4 for July still immature. Recent cohorts retain as well despite the ~90% acquisition-volume collapse.)
- Weekend-activated users return at 6% wk1 vs 26% weekday (4x gap) — WHY? Is it a population difference (weekend first-users are a more casual/geographic/channel-skewed cohort) or a support/response gap (no weekday live-response to recover their first-session friction)? Split weekend-vs-weekday births by mp_country_code, initial_utm_source, and device to see if the gap is composition or intrinsic.
- Does a Monday re-engagement nudge aimed at weekend first-users (who are ~one-and-done by construction, ~150/month) recover any of them — i.e. is the 6% weekend wk1 return a fixable timing problem or an intent floor? (needs an experiment / DB-side send list.)
- Does the weekend-birth retention penalty also hold on the BUILDER surface (node_used self-retention by day-of-week of first node), or is it specific to conversation-activated users?
- ~~What activity-decay pattern precedes a company going silent (any early-warning signal)?~~ (2026-07-24: there is NO gradual decay to catch at the agent-usage grain — churn is a WEEK-1 CLIFF. Only ~25% of agent-messagers return the following week; the ~75% one-and-done is set in week 1. Survivors then plateau (weekly active ~flat wk2-6, ~79% of wk1 by wk6). The one leading indicator is week-1 no-return; there is no slow-fade cohort. Same cliff-then-plateau shape on the builder, shallower (57% cliff, ~32% plateau). Company-level, not distinct_id, needs Neon/Stripe.)
- Does WEEK-1 INTENSITY (number of active days or messages in the first week) predict which of the ~25% survivors stay past week 4 — i.e. is there a within-week-1 dosage threshold that separates durable from fragile survivors? (needs per-user event counts / JQL groupByUser — not in Mixpanel MCP; revisit with DB.)
- The wk0→wk1 cliff is the whole churn story — what happens IN week 1 for the 25% who return vs the 75% who don't (2nd message same-session? a build? a template install?)? Isolating the week-1 action that separates the two cohorts is the highest-leverage retention question.
- Does the cliff-then-plateau hold at the COMPANY grain (company_id-deduped) or is it a distinct_id artifact — do companies show a gentler multi-week decay that user-level self-retention hides? (needs company dedup / DB.)
- Among resurrected (dormant→active) companies, what event marks the comeback?
- Peru is the #4 agent-volume market yet returns at ~3% wk1 (vs Ecuador 38%). Is that near-zero retention driven by one/few churned accounts, or broad across Peruvian companies? (needs company_id dedup — no JQL groupByUser in Mixpanel MCP; revisit when DB/Stripe wired.)
- Why does Ecuador retain agent usage ~2.3x better than Colombia/Mexico (38% vs 16-17% wk1)? Product-language/localization, sales-touch, account size, or self-serve-vs-managed mix by market? (2026-07-28 partial: the self-serve-vs-managed-MIX explanation is RULED OUT via Mixpanel — self_service_cohort tags only 1.2% (42/3,506) of agent-active users and cohort_tag is 0% populated, far too sparse to move a market average; and the geography rates re-confirmed durable on an independent window (EC 39/CO 16/MX 17/PE 3 wk1). Still open: language/localization vs sales-touch vs account-size — needs a populated plan/tier field + company grain from Neon/Stripe.)
- Retention concentrates the base MORE than acquisition: Ecuador is 43% of acquired agent users but ~70% of week-1-retained (Peru 12%→1.4%). Does REVENUE concentrate the same way — is Ecuador ~70%+ of credit_topup / paid revenue, and is Peru's ~400-users/quarter volume essentially unmonetized? (needs Stripe)
- Peru leaks ~149 week-1-retained agent users/quarter vs Ecuador's rate. Is that near-total ~3% wk1 return one/few churned accounts or broad across Peruvian companies, and does the same collapse show on the builder (node_used) surface or only on agent chat? (needs company_id dedup / DB)
- Only 1.2% of agent-active users carry any self-serve/plan tag in Mixpanel — is the plan/tier field better populated in Neon, and can we backfill self_service_cohort onto the $user profile so plan-based retention/monetization splits become answerable in Mixpanel?
- Does the geographic retention gap (EC ≫ CO/MX ≫ PE) hold on node_used (builder) cohorts too, or is it specific to agent messaging?
- Does per-market agent-usage retention track per-market revenue (credit_topup / Stripe) — i.e. is Ecuador's stickiness monetized, and is Peru's volume worthless? (needs Stripe)

## Usage depth
- ~~What share of all workflow executions come from the top 1% / 10% of companies?~~ (2026-07-07: agent_message_sent proxy — top 1% of companies = 28.7%, top 10% = 61.7%, Gini 0.70; 48% of companies sent ≤2 msgs in 90d. Confirm on true ClickHouse executions when wired.)
- Do companies with high workflow error_rate_pct churn faster than clean-running ones?
- ~~Does building SKILL-type nodes (vs only tools) mark a stickier user?~~ (2026-07-16: reframed to the skill lifecycle — skills are created prolifically but rarely deployed. 761 users / 542 companies created 2,894 skills in 90d, but only ~1 in 4 companies (28%; 12% on a strict 30d ordered funnel) ever adds a skill to the canvas; 57% of creators are one-and-done. Not a device artifact — creation is 92% desktop. Open: do the ~25% who DEPLOY retain/monetize better than create-only users?)
- Do skill-DEPLOYERS (skill_used) retain and monetize better than create-only users? Needs $user-profile company_id join to agent_message_sent / credit_topup (agent_message_sent has no event-level company_id).
- Why is the create→canvas step leaking 72–88%? Are skills being created AI-assisted in bulk (2,894 skills / 761 users) as throwaway experiments, or is the canvas-drop UX the blocker? Cross-check with node_config / tester events.
- Does a created-but-never-deployed skill predict churn faster than never-creating a skill at all (i.e. is abandoned exploration a worse signal than no exploration)?
- Are the top-1% "power" companies (30 accounts, ~29% of agent volume) the same ones that pay, and what's the revenue risk if any churns? (needs Stripe)
- Does the Ecuador concentration (50.5% of agent usage) match revenue concentration, or do other markets monetize better per active company? (needs Stripe)
- Within the top-decile companies, how many distinct active users per company — is depth driven by a few power-users or broad team adoption?
- What separates the 65% of builders who add <5 nodes then stop from the 6% who add ≥40 — is there an early node-count threshold that predicts becoming a power account? (2026-07-27 partial: the depth cliff is the 2nd action — 30% of builders never add a 2nd node, then step-conversion RISES monotonically to 91% by node 8 (survivorship ramp); skills 2x steeper, 59% never make a 2nd. The threshold that "locks in" a builder is ~4-5 nodes. Still open: does crossing node #2 causally predict power-account/pay, or just mark it?)
- The depth cliff is a distinct_id read (30% node one-and-done, 59% skill one-and-done). Does it hold at COMPANY grain — i.e. do companies whose nodes/skills spread across teammates show a gentler 1st→2nd cliff? (needs company_id dedup / DB.)
- Node 1→2 is both the biggest drop AND the slowest step (~2.6 days for nodes, ~5.8 days for skills). Is the multi-day gap "left and came back later" (a re-engagement/nudge opportunity) or "same-session but slow"? Split the 1st→2nd interval into same-session vs cross-session to size an in-first-session-2nd-node nudge.
- Do builders who cross the 2nd-node cliff retain (wk2/wk4) and monetize (credit_topup) materially better than one-and-done builders — is "placed a 2nd node" the depth analogue of the week-1 return cliff as a leading indicator?
- Why do skills stall 2x harder than nodes at the 2nd step (41% vs 70% continue)? Is a single skill genuinely useful on its own (so no need for a 2nd) while a single node is not, or is skill-authoring friction higher after the first?

## Cross-source / wildcard
- Which company has the highest assistant build cost (Neon) relative to realized executions (ClickHouse)?
- ~~Is there an acquisition channel that is cheap to sign up but never pays (a low-quality source)?~~ (2026-07-09: at the pre-pay/build-activation proxy, Facebook_Mobile_Reels is the clearest low-quality source — 151 signups, 0.7% ever build. Confirm on realized pay once Stripe is wired.)
- Does the Facebook_Desktop_Feed placement (30% build vs organic 16%) scale — is there enough desktop-placement inventory on Meta to shift spend into, or is it a small accidental slice?
- What is the realized pay-through (credit_topup / Stripe) by placement — do cpc/ppc (Google, ~6-7% build) ever recover CAC despite low build rates, or are they as dead as Meta mobile?
- Is the ~90% signup_completed drop since Jul 3 a broken tracking event or a real signup-funnel outage? Cross-check against server-side signups / DB user-created rows once wired.
- ~~If signup_completed is broken, since exactly when, and are onboarding_started / first-session events still firing for brand-new distinct_ids (which would confirm tracking-only, not a real outage)?~~ (2026-07-20: onboarding_started is NOT still firing normally — it ALSO collapsed on Jul 3, prod last-7d -67% (420 vs 1,264), so the break is the whole new-user onboarding funnel, not just the signup_completed event. Weakens the pure-tracking read; could be a real top-of-funnel/acquisition outage OR a shared onboarding-flow instrumentation regression. Still needs a new-vs-returning distinct_id split + server-side counts to fully close.)
- ~~Now 17 days sustained (Jul 3→20) with no recovery — has anyone confirmed whether real new users are still arriving?~~ (2026-08-06, day 34: YES — real new users still arrive and activate. Over Jul10–Aug5, 685 onboarding-starters converted to an agent message within 7d at 17% (117 users) vs 29% pre-collapse, and app_session_started runs at 100% of its June baseline (~6.3–6.6k/day). NOT a total outage. Reframed: the ~90% daily signup drop = ~80% fewer users ENTER onboarding (onboarding_started 127→25/day unique) × per-user onboarding→signup completion falling only 44%→28% (still fires, so NOT a cleanly-dead signup tag as Jul-9 suspected). Whether the ~80% onboarding-entry drop is real lost acquisition vs a shared upstream onboarding_started tracking break still needs server-side user-created rows (Neon, deferred).)
- Since signup_completed STILL fires for 28% of onboarding-starters (not ~0), it is not a fully-broken tag — is the 44%→28% per-user completion drop a genuine partial regression on signup_completed, or are those "missing" 16pts real abandoners who now bail at the final step? (needs server-side signup rows to separate.)
- Is the ~80% drop in onboarding_started UNIQUE users (127→25/day) real lost top-of-funnel acquisition, or is onboarding_started itself partially under-firing? Decisive test needs a NEW-vs-returning distinct_id split on first-ever events, or Neon user-created timestamps (deferred).
- Post-collapse onboarding→agent 7d activation fell 29%→17% — are the smaller post-Jul-3 new-user inflow LOWER quality (activating worse), or is the 17% a window-maturity/attribution artifact of the reduced volume? Re-check once the post cohort fully matures.
- The onboarding_started→signup_completed same-day ratio itself halved (43%→19%) on top of the -67% onboarding drop — is the extra completion loss a second, distinct regression on signup_completed specifically, or a real drop in people finishing signup?
- The untagged 'undefined' home_variant fell hardest (-91% vs home_a/home_b ~-70%) — does that localize the break to a specific entry path (app-direct / a particular referral / a landing page that doesn't set home_variant)?
- Do new distinct_ids still appear in node_used / agent_message_sent after Jul 3 (i.e. are new users still arriving despite the flat signup event)? (2026-07-09: node_used/agent_message_sent/tester_session_started all recovered to normal volume Jul 6-8 while signups stayed floored — points to a broken signup_completed event, not a real new-user outage. Still needs new-vs-returning distinct_id split to confirm.)
- Since exactly which deploy/commit did signup_completed break (Jul 2→3 boundary)? Bisect against release history once repo/CI access is available.
- Weekly agent_message_sent UNIQUE users slid 488 (wk Jun 22) → 391 (Jun 29) → 215 (Jul 6) even though node_used total stayed healthy (~1.5-1.7k/wk). Is agent-MAU genuinely shrinking, or is it a knock-on of the signup_completed collapse (fewer new users → fewer first agent messages), or a second tracking regression? Split new-vs-returning distinct_ids to disentangle.
- agent_message_sent was only instrumented ~May 18 2026 (0 unique users before). What was the pre-May-18 agent-usage event (if any), and can earlier agent history be recovered for longer retention cohorts?

## Acquisition — signup path composition (added 2026-07-29)
- Google SSO is the dominant signup path (June: 1,377 SSO-starts vs 409 email-submits of 3,814 onboarding_started; SSO completes 72%). What's the SSO→signup completion trend, and is the ~64% of onboarding_started who never even start SSO the real top-of-funnel leak to attack (vs any within-email micro-step)?
- The email/password path sheds 60% at the email→password step (409→162), and only 7% of that drop is users switching to SSO — so it's genuine abandonment. WHY do email-path users bail after typing an email but before committing a password (form friction, password rules, second thoughts, bot/abandoned-email noise)? Needs session replay / UX, not Mixpanel counts.
- company_profiling_step_completed was only instrumented ~Jul 1 2026 — once it has clean post-collapse history, does the profiling step add a third material leak, and does it differ SSO vs email-path?
- Does signup-path choice (Google SSO vs email/password) predict downstream activation/retention (do SSO signups build/converse/retain differently than email-path signups)? Cohort on path event, measure node_used / agent_message_sent within 14-30d.

<!-- appended 2026-07-31 (template shelfware / install-exercise) -->
- Does formally TESTING an installed template (tester_session_started within 30d) predict downstream retention/pay better than merely conversing with the agent — i.e. is the test step the real activation milestone for installers, worth a nudge for the ~57% who skip it?
- Why do US template installers run the agent at 81% but formally test at only ~22% (2/9) — is this a genuine behavior (they trust/ship without testing) or a small-N artifact? Re-check when the US installer base grows.
- Company-dedup the install→test funnel (distinct_id → company_id) to see whether the 43% test-rate is a per-company or per-user leak, and whether Ecuador's 55% edge survives dedup.
- Does the install→test leak differ by WHICH template was installed (the ~40 sprawled custom/Spanish-named templates vs the ~8 curated gallery ones)?

<!-- appended 2026-08-05 (usage-depth power-law: build vs agent, average-breaks) -->
- The build surface (node_used) concentrates HARDER than conversation (top-10% companies = 72% vs 63%, Gini 0.79 vs 0.71). Is that because building is genuinely reserved for a power-user core, or because a few large/internal accounts (e.g. company_id 135 = 914 node-adds, 331 = 1,069 agent msgs) dominate? Strip identifiable internal/test company_ids and re-run to see how much concentration is real customers.
- Does the ~10% power-user core on node_used OVERLAP with the ~10% core on agent_message_sent (same companies deep on both surfaces), or are builders and conversers largely disjoint account sets? Join the two per-company distributions.
- Power-law of actual workflow RUNS (ClickHouse gold.workflow_executions_weekly / root_tool_executions) vs node-ADD concentration measured here — do companies that build many nodes also EXECUTE proportionally, or does execution concentrate even harder (a few production agents carrying all the runs)? Needs ClickHouse (deferred).
- Median company adds just 3 nodes and 29% add exactly one (one-and-done builders, mirrors the Jul-27 node depth-cliff). Do the bottom-50% (4.8% of node-adds) ever RETURN to build again, or is a single node the terminal state? Cohort the 1-node companies and measure any 2nd node_used at 30/60/90d.
- Does workflow error_rate_pct differ between the top-decile power-users and the long tail (do heavy builders run cleaner, or do they hit more errors because they build more complex graphs)? Needs ClickHouse error_rate_pct (deferred, today's assigned primary).

<!-- appended 2026-08-10 (first-milestone predicts 2nd-week return: build/test >> chat) -->
- The best-predicting first milestone (node/tester) beats the widest one (agent chat) on 2nd-week return, but node/agent births aren't first-ever (event-age). Does the ordering survive a TRUE first-milestone cohort — each user's genuinely first among {node, tester, agent, skill} via JQL groupByUser / a DB user-created join — or is node-first inflated by veterans re-engaging?
- Is the build/test-beats-chat return gap a MARK (deeper actions self-select committed users) or a DRIVE (getting a user to build/test in week 1 causally lifts their return)? Test with users who chat-first then build vs chat-first only: does a later first build in week 1 raise the chat cohort's 2nd-week return toward the builder curve?
- Where does skill_created rank as a first milestone for 2nd-week return (add it to the node/tester/agent head-to-head), and does doing MULTIPLE distinct first-week milestones (build AND test AND chat) predict return better than any single one — i.e. is milestone BREADTH the real signal?
- The agent-first cohort is the widest (65% of signups) and the weakest 2nd-week returner (~46% app-session / 24% self) — can an in-first-session nudge that converts a chat-first user into also placing a node or starting a tester session move their return rate, and is that the highest-volume retention lever given the size of the chat-first cohort?
- Does the build/test >> chat 2nd-week-return ordering hold at COMPANY grain (company_id-deduped) and by geography (does Ecuador's retention edge concentrate in build/test-first users)? Needs company dedup / DB.

<!-- appended 2026-08-11 (per-template install->activation: English flagships vs Spanish duplicates) -->
- Per-template install->activation splits 25%-83% (tester, 30d). Is the English-flagship-vs-Spanish-duplicate activation gap ('Identity Verification or Biometrics' 58% vs 'Validacion de identidad con Biometria' 25%; 'Schedule with Google Calendar' 83% vs 'Asistente de Agendamiento' 40%) a COMPOSITION effect (English templates installed by more sophisticated/international builders) or an INTRINSIC template-quality effect? Split each template's installers by mp_country_code / initial_utm_source / self-reported segment to see if the gap survives holding the cohort fixed.
- Why do near-duplicate templates exist at all (English + Spanish, casing variants like 'Validacion de identidad/Identidad con Biometria')? Do the low-activating Spanish duplicates represent gallery clutter that should be consolidated into the higher-activating canonical templates — quantify installers 'lost' to the weaker duplicate vs the flagship.
- Two templates activate SAME-SESSION (Google Calendar ~2.2h, Jelou Shop in-chat payment ~2.4h) vs ~3-6 days for the Shopify/Identity workhorses. Is fast time-to-first-tester CAUSAL for stickiness (does a template that gets the user testing within the first session retain better downstream), or does it just mark a simpler template? Cohort by time-to-first-tester bucket and measure 2nd-week return.
- Does the per-template activation ranking predict per-template PAY-through (credit_topup / Stripe)? The flagship commerce templates (Shopify/Jelou Shop in-chat payment) are payment-adjacent by design — do their installers top up at a higher rate than FAQ/scheduling installers? Needs credit_topup-by-template + Stripe (deferred).

<!-- appended 2026-08-13 (resurrection: re-engagement concentrates on the build surface, not chat) -->
- Chat-activated users' weekly return-via-BUILD overtakes return-via-chat by ~wk3-4 (crossover) and decays 1.6x slower, but the chat cohort has 40% wk0 build overlap so this is not pure-chatter migration. Isolate the ~60% of the chat cohort that did NOT build in wk0 (chat-ONLY activators) via JQL groupByUser / a DB cohort, and re-run their return-via-build curve: do genuinely chat-only users ever migrate to the canvas, or is the crossover entirely the always-been-builders subset re-surfacing?
- The cross-surface 2x2 is distinct_id, unbounded weekly (conflates continuous retention with true dormant->return). Rebuild it as a GAP-defined resurrection cohort: users active in an early window, then >=2 (and >=4) fully-silent weeks, then any event -- and measure which surface (node/agent/tester/template/campaign/billing) their FIRST comeback event lands on. Needs bounded retention or JQL groupByUser (MCP retention report exposes neither).
- Does the build-is-the-comeback-surface pattern hold at COMPANY grain (company_id-deduped)? A dormant company might resurrect through a DIFFERENT teammate on a different surface than the one who went quiet -- per-user surface-return likely understates company-level cross-surface resurrection.
- What actually TRIGGERS a resurrection in time order (the standing Jul-15 open question)? With no JQL groupByUser in the MCP, try a Flows-INTO-node_used / Flows-INTO-agent_message_sent report filtered to sessions preceded by a long inactivity gap, or a DB event-sequence join, to name the immediate predecessor of a comeback (billing page? template install? an inbound end-user message? a Jelou-side notification?).
- Builders return-to-chat (31% wk1) beats chatters' own return-to-chat (25%) -- building marks the stickier population on every surface. Is there a causal 'build early -> retain everywhere' lever, or pure self-selection? Test whether chat-first users who are nudged to place a first node shift their whole return profile toward the builder curve (ties to the Aug-10 first-milestone backlog).
